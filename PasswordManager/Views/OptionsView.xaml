<UserControl x:Class="PasswordManager.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            
            <RowDefinition Height="Auto" />
            
            <RowDefinition Height="*" />
            
        </Grid.RowDefinitions>
        
        <TextBlock Text="Opcje szyfrowania" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20" 
                   HorizontalAlignment="Center" />
        
        <StackPanel>
        <TextBlock Text="Metoda szyfrowania:"
                   Grid.Row="0"
                   VerticalAlignment="Center"
                   FontSize="14"
                   Margin="0,0,0,5" />
        <ComboBox x:Name="EncryptionMethodComboBox"
                  Grid.Row="1"
                  Width="200"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Margin="0,5,0,0" SelectionChanged="EncryptionMethodComboBox_SelectionChanged">
           
            <ComboBoxItem Content="AES" IsSelected="True" />
                <ComboBoxItem Content="HMAC" />
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>