<UserControl x:Class="PasswordManager.PasswordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- Tabelka -->
            <RowDefinition Height="Auto" />
            <!-- Przyciski -->
        </Grid.RowDefinitions>

        <!-- Tabelka z hasłami -->
        <DataGrid x:Name="PasswordsGrid" Grid.Row="0" AutoGenerateColumns="False" CanUserAddRows="False" 
                  SelectionMode="Single" Margin="0,0,0,10" 
                  IsReadOnly="True" SelectionChanged="PasswordsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Przyciski na dole -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button x:Name="btnAddNewPassword"  Content="Dodaj nowe" Width="100" Margin="5" Click="btnAddPassword_Click" />
        </StackPanel>
    </Grid>
</UserControl>