<UserControl x:Class="PasswordManager.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
          
            <RowDefinition Height="Auto" />
           
            <RowDefinition Height="*" />
           
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
           
            <ColumnDefinition Width="Auto" />
           
            <ColumnDefinition Width="*" />
           
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
               
                <RowDefinition Height="20" />
              
                <RowDefinition Height="Auto" />
               
                <RowDefinition Height="20" />
               
                <RowDefinition Height="Auto" />
               
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
              
            </Grid.ColumnDefinitions>

            
            <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
                <ComboBox x:Name="cbxProfileSelector" Width="200" Height="30" Margin="0,0,10,0" 
                          VerticalAlignment="Center"/>
                <Button x:Name="btnNewProfile" Content="Nowy profil" Width="100" Height="30" VerticalAlignment="Center" Click="btnNewProfile_Click"/>
            </StackPanel>

            
            <PasswordBox x:Name="PasswordField" Grid.Row="2"  Height="30" HorizontalAlignment="Stretch"/>

            
            <Button x:Name="btnLogin" Content="Zaloguj" Grid.Row="4" Width="100" Height="30" HorizontalAlignment="Right"
                    Margin="0,0,0,0" Click="btnLogin_Click"/>
        </Grid>
    </Grid>
</UserControl>